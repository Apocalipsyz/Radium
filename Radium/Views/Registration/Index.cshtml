@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="jumbotron jumb-marg">
    <div class="container cont-left">
        <div class="h1-help">
            <h1>Регистрация</h1>
        </div>
        <p>бла-бла-бла бла-бла-бла бла-бла-бла бла-бла-бла бла-бла-бла бла-бла-бла бла-бла-бла </p>
    </div>
    <div class="container cont-right">
        @using (Html.BeginForm("Index", "Login", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            <div class="form-group">
                <div class="controls">
                    <div class="col-xs-8">
                        <input type="text" class="form-control" placeholder="Имя пользователя" />
                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="controls">
                    <div class="col-xs-8">
                        <input type="text" class="form-control" placeholder="Email" />
                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="controls">
                    <div class="col-xs-8">
                        <input type="text" class="form-control" placeholder="Пароль" />
                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="controls">
                    <div class="col-xs-8">
                        <input type="text" class="form-control" placeholder="Повторите пароль" />
                    </div>
                </div>
            </div>
            <div class="form-actions form-marg">
                    <button type="submit" class="btn btn-primary">
                        Регистрация
                    </button>
            </div>
        }
    </div>
</div>
